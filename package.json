{
  "name": "TMON_TEST",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss-compile" : "node-sass -w src/scss/style.scss --output ./css",
    "build": "npx babel --watch src --out-dir . --presets react-app/prod",
    "start": "npm run scss-compile & npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react-app": "^3.1.2",
    "moment": "^2.29.1"
  },
  "devDependencies": {
    "node-sass": "^5.0.0"
  }
}
